services:
  mongo:
    image: mongo:7
    container_name: intern-auto-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - intern_auto_mongo_data:/data/db

volumes:
  intern_auto_mongo_data:
